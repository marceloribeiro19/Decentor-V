# Decentor-V
Shifting Machine Learning Training to RISC-V devices

to compile the code the project folder must be inside gap_sdk(3.8.1) release repository -> examples -> pmsis and follow the "notas.txt" file inside the project
