se der printf com variaveis la dentro da undifined skbr. Soluçao: Incluir o pmsis.h nesse ficheiro.

para flashar o ficheiro: 1º na pasta do codigo dar "make all" para flashar a imagem, 2º dar run

com o freertos fica preso em em pi_fs_mount() com pulpos nao

para usar o pulos tem de se remover do make file a flag "-lc"

----------------------------------------implementaçao--------------------------------------------
-as funçoes math.h sao muito lentas principalmente a pow()

-A pulp-nn é optimizada para varios cores então acho que isto frita quando se usa como output da layer
um vetor com "1" de dimensão que é o caso da minha ultima layer, então tive de fazer dentro da funçao
da pulp um handle case para quando o output so tem 1 neuronio

A LUT foi gerada previamente utilizando fracbits = 4 (Q3.4) e quantizada em valores entre -128 / 128 de maneira que para desquantizar tenho de dividir por 128.

interpolaçao linear na LUT??????????????

-Accuracy estagnada: Tava a ter um problema que se chama "Dying ReLU" que é quando temos valores negativos no calculo da derivada do ReLU ele teoricamente 
deveria returonar zero (porque relu sao valores entre 0 e 1) mas na pratica quando se faz isso no treino como se multiplica o learning rate e os pesos por 
ero o modelo acaba por não aprender e ficar sempre com a mesma accuracy causando os chamados "dead neurons"